<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meditracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitati.AdaugareDiagnosticActivity"></activity>
        <activity android:name=".activitati.VerificarePacientActivity" />
        <activity android:name=".activitati.PacientiActivity" />
        <activity android:name=".activitati.InternariActivity" />
        <activity android:name=".activitati.TratamentActivity" />
        <activity android:name=".activitati.StatisticiSectieActivity" />
        <activity android:name=".activitati.AdaugarePacientActivity" />
        <activity android:name=".activitati.FeedbackActivity" />
        <activity android:name=".activitati.ProfilActivity" />
        <activity
            android:name=".activitati.MainActivityPacient"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activitati.MainActivityUPU"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activitati.AdaugareAngajatActivity" />
        <activity android:name=".activitati.LoginActivity" />
        <activity
            android:name=".activitati.StartActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitati.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activitati.MainActivityAdmin"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>